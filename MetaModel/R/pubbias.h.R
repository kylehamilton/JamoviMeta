
# This file is automatically generated, you probably don't want to edit this

#' @importFrom jmvcore Options
#' @importFrom R6 R6Class
PubBiasOptions <- R6::R6Class(
    "PubBiasOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            yi = NULL,
            vi = NULL, ...) {

            super$initialize(
                package='MetaModel',
                name='PubBias',
                requiresData=TRUE,
                ...)
        
            private$..yi <- jmvcore::OptionVariable$new(
                "yi",
                yi)
            private$..vi <- jmvcore::OptionVariable$new(
                "vi",
                vi)
        
            self$.addOption(private$..yi)
            self$.addOption(private$..vi)
        }),
    active = list(
        yi = function() private$..yi$value,
        vi = function() private$..vi$value),
    private = list(
        ..yi = NA,
        ..vi = NA)
)

#' @import jmvcore
#' @importFrom R6 R6Class
PubBiasResults <- R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$..text),
    private = list(
        ..text = NA),
    public=list(
        initialize=function(options) {
            super$initialize(options=options, name="", title="Publication Bias")
            private$..text <- jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Publication Bias")
            self$add(private$..text)}))

#' @importFrom jmvcore Analysis
#' @importFrom R6 R6Class
PubBiasBase <- R6::R6Class(
    "PubBiasBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'MetaModel',
                name = 'PubBias',
                version = c(1,0,0),
                options = options,
                results = PubBiasResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE)
        }))

#' Publication Bias
#'
#' 
#' @param data .
#' @param yi .
#' @param vi .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#' }
#'
#' @export
PubBias <- function(
    data,
    yi,
    vi) {

    options <- PubBiasOptions$new(
        yi = yi,
        vi = vi)

    results <- PubBiasResults$new(
        options = options)

    analysis <- PubBiasClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
